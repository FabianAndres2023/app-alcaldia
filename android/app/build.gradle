apply plugin: 'com.android.application'
apply plugin: 'com.google.gms.google-services'  // ✅ Activa Firebase en este módulo

android {
    namespace "tulua.app"
    compileSdk rootProject.ext.compileSdkVersion

    defaultConfig {
        applicationId "tulua.app"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 6
        versionName "1.0.1"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

        aaptOptions {
            // Archivos y directorios a omitir del paquete de recursos
            ignoreAssetsPattern '!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~'
        }
    }

    signingConfigs {
        release {
            storeFile file("my-release-key.keystore")
            storePassword "tuluavalle"
            keyAlias "my-key-alias"
            keyPassword "tuluavalle"
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            shrinkResources false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            debuggable false
        }
        debug {
            // Mantiene el build debug igual
        }
    }

    // ✅ Fuerza compilación con Java 17
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')

    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
    implementation "androidx.core:core-splashscreen:$coreSplashScreenVersion"

    implementation project(':capacitor-android')

    // ✅ Firebase BoM: mantiene compatibilidad entre SDKs
    implementation platform('com.google.firebase:firebase-bom:34.5.0')

    // ✅ Firebase Messaging (Push Notifications)
    implementation 'com.google.firebase:firebase-messaging'

    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
}

apply from: 'capacitor.build.gradle'

// ✅ Verifica existencia de google-services.json
try {
    def servicesJSON = file('google-services.json')
    if (servicesJSON.text) {
        println("✅ google-services.json detectado — Firebase habilitado")
    }
} catch(Exception e) {
    logger.info("⚠️ google-services.json no encontrado. Firebase deshabilitado temporalmente.")
}
